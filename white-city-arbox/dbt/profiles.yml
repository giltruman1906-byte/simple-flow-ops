white_city_arbox:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('SUPABASE_DB_HOST') }}"
      port: 5432
      dbname: postgres
      user: postgres
      password: "{{ env_var('SUPABASE_DB_PASSWORD') }}"
      schema: public
      threads: 4
